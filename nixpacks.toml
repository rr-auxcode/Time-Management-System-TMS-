# Nixpacks configuration for Railway
# This helps suppress npm warnings

[phases.setup]
nixPkgs = ["nodejs-18_x", "npm"]

[phases.install]
cmds = [
  "npm ci --omit=dev"
]

[phases.build]
cmds = [
  "npm run build"
]

[phases.start]
cmd = "npm start"
